classdef ValidateableObj < handle
    %VALIDATEABLEOBJ Adds validation to the object, with timed validation.
    
    properties (Access = private)
        timedv={};
        default_timed_v='__ValidateableObj__base_v';
    end
    
    methods
    end
end

